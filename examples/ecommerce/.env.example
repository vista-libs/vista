# PostgreSQL — matches the Docker container started by npm run db:start
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ormai_dev"

# OpenRouter API key — get yours at openrouter.ai/keys
OPENROUTER_API_KEY="sk-or-..."
